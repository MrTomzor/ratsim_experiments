# Track the canonical machine configs; ignore any extras the user adds locally.
*
!.gitignore
!default.yaml
!gpu_example.yaml
# rci.yaml is a shared CTU cluster, not a personal box
!rci.yaml
!README.md
